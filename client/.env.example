# Production API base URL (your deployed backend)
# In development the Vite proxy handles /api requests automatically — this is NOT needed locally
VITE_PROD_API_URL=https://your-backend.onrender.com/api

# Production Socket.IO server URL (cannot use proxy for WebSockets)
VITE_SOCKET_URL=https://your-backend.onrender.com
